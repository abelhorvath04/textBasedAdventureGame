package org.lecture.Room;

/**
 * The RoomType enum represents the types of rooms in the game.
 * Each room type has its specific characteristics and events.
 */
public enum RoomType {
    START,
    END,
    ORDINARY,
    MAGIC,
    TRAP,
    TAVERN
}
